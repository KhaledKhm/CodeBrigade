{% extends 'base2.html.twig' %}
 {% block content %}  {% endblock  %}
{% block body %}
    <div class="page-content">

    <section id="blog" class="content-section" style="width: 1200pt" >
        <div class="section-heading" >
            <strong> <h1>Liste des<br><em>Evènements</em></h1> </strong>
        </div>

        <div class="section-content" style="margin-right:50px" >
            <div class="tabs-content" style="margin-top:0px">
                <div class="wrapper">
                    <ul class="tabs clearfix" data-tabgroup="first-tab-group">
                        <li><a href="#tab1" class="active">1</a></li>
                        <li><a href="#tab2">2</a></li>
                        <li><a href="#tab3">3</a></li>
                        <li><a href="#tab4">4</a></li>
                    </ul>
                    {% for Evenement in classe %}
                    <section id="first-tab-group" class="tabgroup">
                        <div id="tab1">
                            <ul>
                                <li>
                                    <div class="item">
                                        <img src="img/blog_2.jpg" alt="">
                                        <div class="text-content">
                                            <h4> {{Evenement.libelle}}</h4>
                                            <span>  {{ Evenement.DateDebut}}</span>
                                            <p>{{ Evenement.description}}</p>

                                            <div class="accent-button button">
                                                <form action="{{ path('ajoutPart1',{'id':Evenement.id} )}}"><button  class="btn btn-outline-info"><h6>Participer</h6></button></form>
                                            </div>
                                            <div class="accent-button button">
                                                <a href="#contact">Continue Reading</a>
                                            </div>
                                        </div>
                                    </div>
                                </li>

                            </ul>
                        </div>


                    </section>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>

{% endblock %}